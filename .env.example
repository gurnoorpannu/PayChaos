# Optional. PayChaos runs with its grounded local analyzer when this is empty.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4

# Optional. Test Mode credentials only; rzp_live_ keys are rejected.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
